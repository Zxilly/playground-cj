# Exercise: Errors

Implement a `sqrt` function that returns `Option<Float64>`:
- Return `Some(result)` when the input is non-negative
- Return `None` when the input is negative

Then write a `safeSqrt` function that uses `??` to return `0.0` as the default for `None`.
