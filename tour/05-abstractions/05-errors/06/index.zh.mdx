# 练习：安全除法

将 `divide` 函数从抛出异常改为返回 `Option<Float64>`：
- 当除数非零时，返回 `Some(结果)`
- 当除数为零时，返回 `None`

然后实现 `safeDivide` 函数，使用 `??` 操作符在遇到 `None` 时返回默认值 0.0。

<Note>`??` 操作符可以处理 `None` 情况，让代码更简洁。例如 `opt ?? default` 在 `opt` 为 `None` 时返回 `default`。</Note>