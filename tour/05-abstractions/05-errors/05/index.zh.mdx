# Option 与异常

当值的缺失是正常且预期的结果时（例如查找可能不存在的键），使用 `Option<T>`。对于真正意外的情况（表示程序错误或不可恢复的故障），则使用异常。

返回 `Option<T>` 的函数强制调用方显式处理缺失情况。而异常会自动沿调用栈向上传播，直到被捕获。

将 `divide` 函数改为返回 `Option<Float64>` 而非抛出异常，并用 `??` 处理 `None`。
