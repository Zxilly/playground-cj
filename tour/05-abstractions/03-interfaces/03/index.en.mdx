# Polymorphism

A variable of interface type can hold any implementing object.

The actual method called depends on the runtime type, not the declared type.

Here, `launch` takes a `Flyable`. Passing a `Bird` or `Airplane` produces different output from the same function call.

Add a `Drone` class that implements `Flyable` and pass it to `launch`.
