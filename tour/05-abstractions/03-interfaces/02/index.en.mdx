# Implementing Interfaces

A class implements an interface by declaring `<:` and providing all required methods.

Multiple classes can implement the same interface with different behavior.

In this example, both `Dog` and `Athlete` implement `Runner`. The `showRun` function accepts any `Runner`.

Add a `Robot` class that implements `Runner` and pass it to `showRun`.
