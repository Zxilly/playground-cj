# Any Type

`Any` is the top type -- every type satisfies it. It is like an empty interface.

Use pattern matching with type patterns to recover the concrete type from an `Any` value.

Add a `Float64` case to the `match` in `printValue` and call `printValue(3.14)`.
