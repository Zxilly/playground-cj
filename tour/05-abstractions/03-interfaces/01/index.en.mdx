# Defining Interfaces

An _interface_ declares a set of method signatures.

Any type that implements those methods satisfies the interface. Interfaces can also provide default implementations.

In this example, `Printable` requires `display()`, while `Named` provides a default `name()` that returns `"anonymous"`.

Remove the `name()` override from `User` to see the default `"anonymous"` returned.
