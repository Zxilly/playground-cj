# Lambda Expressions

The syntax `{ params => body }` creates an anonymous function. Use `{ => body }` when there are no parameters. The last expression is the return value.

In this example, `double` and `abs` are lambdas assigned to variables.

Try changing the lambda on line 7 to `{ x: Int64 => x * x }` and run.
