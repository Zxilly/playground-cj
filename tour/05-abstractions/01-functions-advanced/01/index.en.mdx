# Functions as Values

Functions in Cangjie have types, like `(Int64) -> Int64`. They can be assigned to variables and passed as arguments to other functions.

In this example, `double` and `square` are passed to `apply`, which calls them.

Try assigning `square` to `fn` on line 14 and run the program.
