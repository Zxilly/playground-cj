# Nested Functions

Functions can be defined inside other functions. The inner function can access the outer function's local variables and is only visible within the enclosing function.

In this example, `sum` and `average` are defined inside `processNumbers`.

Try adding a nested `max` function and returning it as a third value.
