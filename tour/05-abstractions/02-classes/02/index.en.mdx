# Constructors

A class uses `init` to define constructors. Multiple `init` blocks with different parameter lists provide different ways to create instances.

In this example, `Person` has two constructors. The second one defaults `email` to `"unknown"`.

Try adding a third constructor that takes only `name` and defaults both `age` to `0` and `email` to `"unknown"`.
