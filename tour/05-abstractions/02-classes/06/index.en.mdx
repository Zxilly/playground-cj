# Abstract Classes

An `abstract class` declares methods without a body. Subclasses must implement all abstract methods. An abstract class cannot be instantiated directly.

In this example, `Shape` declares `area()` and `label()` as abstract. The concrete `describe()` calls them, dispatching to the subclass at runtime.

Try writing `Shape()` in `main` and observe the compile error.
