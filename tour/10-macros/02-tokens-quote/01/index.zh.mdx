# Token 令牌

`Tokens` 是由 `Token` 对象组成的序列，表示一段源代码片段。每个 `Token` 有一个 `kind`（如 `TokenKind.IDENTIFIER` 或 `TokenKind.INTEGER_LITERAL`）和一个字符串 `value`。

可以用 `Token(kind, value)` 构造 Token，用 `+` 拼接。

将第一个 Token 中的 `"1"` 改为 `"42"`，运行程序查看更新后的输出。
