# Array Operations

Array elements can be modified by index: `numbers[1] = 25`. Sub-arrays are obtained with a range slice: `numbers[1..3]`.

Use `for (i in 0..numbers.size)` to iterate with indices.
