# Arrays

`Array<T>` is a fixed-size sequence of elements of the same type. The literal syntax is `[1, 2, 3]`.

Elements are accessed by zero-based index. The `.size` property returns the number of elements.

Try accessing `numbers[10]` and observe the runtime error.
