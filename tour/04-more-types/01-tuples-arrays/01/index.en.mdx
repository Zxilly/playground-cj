# Tuples

`(Int64, String)` is a tuple type. A tuple groups values of different types into one compound value.

Access elements by index: `t[0]`, `t[1]`. Tuples must have at least two elements. They are immutable -- you cannot modify individual elements after creation.

Try to assign `point[0] = 10` and observe the error.
