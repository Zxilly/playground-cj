# Exercise: Word Count

Implement a word counter. It should return a `HashMap<String, Int64>` of the counts of each word in the input array.

You might find the `getOrDefault` method on `HashMap` helpful.
