# Defining Structs

A `struct` is a collection of fields. Each field has a name and a type. Fields declared with `let` are immutable; fields declared with `var` are mutable.

Struct fields are accessed using dot syntax: `point.x`.

Add a `year` field of type `Int64` to `Book` and print it.
