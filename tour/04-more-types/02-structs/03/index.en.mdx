# Struct Methods

Functions defined inside a struct are methods. They access the struct's fields through `this`.

Methods are called with dot syntax on an instance: `c1.area()`.

Add a `diameter()` method that returns `2.0 * radius` and call it from `main`.
