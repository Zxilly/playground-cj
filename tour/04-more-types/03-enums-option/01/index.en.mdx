# Basic Enums

An `enum` defines a type with a fixed set of named cases, declared with `|`.

Each case is accessed with the enum name: `Direction.North`. Use `match` to handle each case.

Add a `Northeast` case and handle it in the `match` expression.
