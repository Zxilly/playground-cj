# Enums with Data

Enum cases can carry associated data. Each case may hold different types and numbers of values.

Pattern matching with `case Circle(r) =>` extracts the data from each case.

Add a `Square(Float64)` case and handle it in both `area` and `describe`.
