main() {
    var count = 0
    println("初始值: ${count}")

    count = 10
    println("修改后: ${count}")

    count = count + 5
    println("再次修改: ${count}")

    var message = "hello"
    message = "world"
    println(message)
}
