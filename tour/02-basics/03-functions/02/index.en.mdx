# Implicit Return

The last expression in a function body is its return value. No `return` keyword is needed.

```cangjie
func multiply(a: Int64, b: Int64): Int64 {
    a * b
}
```

Try adding `return` before `a * b` on line 2 -- both forms are valid.
