# 程序结构与 main 函数

仓颉程序的执行从 `main` 函数开始。每个可执行程序都必须包含一个 `main` 函数作为入口点。

`main` 函数的基本形式非常简单：函数名后跟一对圆括号 `()`，然后是花括号 `{}` 包围的函数体。函数体中的语句会按顺序依次执行。

如果一条语句独占一行，行末的分号可以省略。但如果一行中有多条语句，则必须用分号 `;` 分隔。

仓颉中，花括号 `{}` 包围的代码称为「代码块」。代码块中的表达式按顺序执行，代码块的值等于最后一个表达式的值。
