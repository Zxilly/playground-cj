# var Mutable Variable

When you need a variable that can be reassigned, use the `var` keyword.

Variables declared with `var` can have their values changed later, but the new value must be of the same type as the original.

While `var` provides flexibility, prefer `let` by default. Only use `var` when you genuinely need to modify a value -- this helps write safer code.
