# Comparison and Logical Operators

`==`, `!=`, `<`, `>=` compare values and produce a `Bool`. `&&` (and), `||` (or), `!` (not) combine boolean values.

```cangjie
let canDrive = age >= 18 && hasLicense
```

Try changing `&&` to `||` on line 21 and see how the result changes.
