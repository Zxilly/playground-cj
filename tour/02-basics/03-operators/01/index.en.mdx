# Arithmetic

Cangjie supports `+`, `-`, `*`, `/`, and `%` (remainder). Parentheses control evaluation order.

```cangjie
let sum = 17 + 5
let rem = 17 % 5  // 2
```

Try adding parentheses to change the evaluation order and predict the result before running.
