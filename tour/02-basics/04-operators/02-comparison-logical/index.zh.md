# 比较与逻辑运算符

**比较运算符**用于比较两个值，返回布尔结果：

- `==` 等于、`!=` 不等于
- `<` 小于、`>` 大于
- `<=` 小于等于、`>=` 大于等于

**逻辑运算符**用于组合布尔表达式：

- `&&` 逻辑与（短路求值）
- `||` 逻辑或（短路求值）
- `!` 逻辑非

短路求值意味着：`&&` 在左侧为 `false` 时不计算右侧；`||` 在左侧为 `true` 时不计算右侧。
