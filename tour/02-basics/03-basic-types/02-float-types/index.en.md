# Float Types

Cangjie provides two floating-point types: `Float32` (32-bit single precision) and `Float64` (64-bit double precision).

Floating-point literals default to `Float64`. They can use decimal notation or scientific notation (`e` or `E`).

For calculations requiring decimal precision, `Float64` is recommended as it provides higher precision.
