# 整数类型

仓颉提供了多种整数类型，分为有符号和无符号两类。

**有符号整数**：`Int8`、`Int16`、`Int32`、`Int64`，分别占用 8、16、32、64 位。

**无符号整数**：`UInt8`、`UInt16`、`UInt32`、`UInt64`，只能表示非负整数。

整数字面量默认推断为 `Int64`。还支持二进制（`0b`）、八进制（`0o`）和十六进制（`0x`）表示，并可用下划线 `_` 分隔以提高可读性。

一般情况下推荐使用 `Int64`，它的表示范围足够大，且能避免不必要的类型转换。
