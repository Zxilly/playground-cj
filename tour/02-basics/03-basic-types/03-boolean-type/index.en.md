# Boolean Type

The boolean type `Bool` has only two values: `true` and `false`.

Booleans are commonly used for conditional checks and logical operations. Cangjie is strongly typed -- conditional expressions (such as `if` conditions) must be of type `Bool`. Unlike C, integers cannot be used as conditions.

The boolean type supports logical operators: and `&&`, or `||`, not `!`.
