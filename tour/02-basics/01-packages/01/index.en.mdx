# Packages and main

Every Cangjie program starts running in the `main` function.

`package` declares which package a file belongs to. This program belongs to the `playground` package.

Try adding a `println` call inside `main` before `return 0`.
