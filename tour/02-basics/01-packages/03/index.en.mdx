# Comments

Cangjie has two kinds of comments. `//` starts a single-line comment. `/* */` wraps a block comment.

Block comments can be nested: `/* outer /* inner */ outer */`.

Try commenting out a `println` line with `//` and see how the output changes.
