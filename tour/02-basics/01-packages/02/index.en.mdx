# Imports

`import` brings declarations from other packages into the current file.

This program imports `ArrayList` from the `std.collection` package. You can also use a wildcard: `import std.collection.*` imports everything from that package.

Try changing the import to `std.collection.*` and verify the program still compiles.
