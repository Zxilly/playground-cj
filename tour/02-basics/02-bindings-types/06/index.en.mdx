# Float Types

`Float64` is the default floating-point type. It represents numbers with a fractional part. Arithmetic on floats works the same way as on integers.

```cangjie
let distance = 5.5
let speed = distance / 2.0
```

Try changing a value and observe how the computed result changes.
