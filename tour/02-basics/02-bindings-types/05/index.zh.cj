main() {
    let days: Int64 = 42
    let decimal = 42
    let hex = 0x2A
    let binary = 0b101010

    println("天数: ${days}")
    println("十进制 42 -> ${decimal}")
    println("十六进制 0x2A -> ${hex}")
    println("二进制 0b101010 -> ${binary}")
}