main() {
    var score = 10
    println("初始分数: ${score}")

    score = 12
    println("重新赋值后: ${score}")

    score += 5
    println("score += 5 => ${score}")

    score *= 2
    println("score *= 2 => ${score}")
}