# Assignment

`=` assigns a new value to a `var` binding. Compound operators like `+=` and `*=` update the value in place.

```cangjie
var score = 10
score += 5  // equivalent to score = score + 5
```

Try rewriting `score += 5` as `score = score + 5` and verify the result is the same.
