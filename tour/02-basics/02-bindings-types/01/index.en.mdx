# let (Immutable)

The `let` statement declares an immutable binding. Once assigned, the value cannot be changed.

Cangjie does not allow variable shadowing in the same scope -- `let x = 1; let x = 2` is a compile error.

Uncomment line 11 to see the compile error when reassigning a `let` binding.
