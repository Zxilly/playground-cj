main() {
    var count = 0
    println("初始值: ${count}")

    count = 10
    println("第一次修改: ${count}")

    count = count + 5
    println("第二次修改: ${count}")

    var message = "hello"
    message = "world"
    println("消息: ${message}")
}