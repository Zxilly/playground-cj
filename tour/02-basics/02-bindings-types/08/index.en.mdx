# Rune

`Rune` represents a single Unicode character. Character literals are written as `r'A'`. Escape characters like `r'\n'` and `r'\t'` represent newline and tab.

```cangjie
let letter: Rune = r'A'
let emoji: Rune = r'🎉'
```

Try changing a character literal to a different letter or emoji.
