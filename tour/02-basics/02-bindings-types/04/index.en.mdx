# Type Inference

When the type is clear from the value, the compiler infers it automatically.

`42` is inferred as `Int64`, `3.14` as `Float64`, `"hello"` as `String`, and `true` as `Bool`.

Try changing the initial value of `x` to `3.14` and observe how its type changes.
