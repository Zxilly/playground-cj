# Type Annotations

A type annotation is written after the name with `: Type`.

The compiler checks that the value matches the declared type. If you omit the annotation, the type is inferred.

Try changing a type annotation to a wrong type (e.g., `Bool` instead of `Int64`) and see the error.
