# Unit and Nothing

`Unit` is the type of expressions that complete but produce no meaningful value, such as `println`. `Nothing` is the type of expressions that never complete normally, such as `break` or `return`.

```cangjie
let result: Unit = println("hello")
break  // type is Nothing
```

Run the code and observe that `println` returns `Unit`.
