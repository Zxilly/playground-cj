main() {
    let distance = 5.5
    let time = 2.0
    let speed = distance / time
    let average = (72.5 + 81.0) / 2.0

    println("距离: ${distance} km")
    println("时间: ${time} h")
    println("平均速度: ${speed} km/h")
    println("两次成绩的平均值: ${average}")
}