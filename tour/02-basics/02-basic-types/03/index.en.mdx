# Bool

`Bool` has two values: `true` and `false`. Logical operators `&&`, `||`, and `!` combine or negate boolean values. Comparison operators like `>` and `==` produce booleans.

```cangjie
let active = true
let result = active && (10 > 5)
```

Try writing a new comparison expression and printing the result.
