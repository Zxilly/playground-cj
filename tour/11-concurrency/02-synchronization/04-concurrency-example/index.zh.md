# 并发综合示例

让我们用一个综合示例来展示仓颉并发编程的各种特性。这个示例模拟了一个简单的并行任务处理系统。

程序创建多个工作线程，每个线程执行不同的计算任务。使用原子计数器跟踪已完成的任务数量，使用互斥锁保护共享的结果列表。

这个示例综合运用了：
- `spawn` 创建线程
- `Future` 获取返回值
- `AtomicInt64` 原子计数
- `ReentrantMutex` 保护共享数据

通过这个示例，你可以看到仓颉的并发原语如何协同工作。