# 共享数据安全

在并发编程中，多个线程同时读写共享数据是最常见的错误来源。仓颉提供了多种工具来确保共享数据的安全：

- **互斥锁**：保护复杂数据结构的读写
- **原子类型**：保护简单数值的读写
- **不可变数据**：使用 `let` 声明的不可变变量天然是线程安全的

最佳实践：
1. 尽量减少共享数据的使用
2. 优先使用不可变数据
3. 必须共享时，使用合适的同步机制
4. 缩小临界区范围，减少锁的持有时间

右侧的代码展示了如何使用互斥锁安全地维护一个共享的列表。
