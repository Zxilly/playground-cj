# 线程取消

在某些场景中，你可能需要取消一个正在运行的线程。仓颉的 `Future` 提供了 `cancel()` 方法来请求取消线程。

线程取消是**协作式**的——调用 `cancel()` 只是发出取消请求，线程需要主动检查取消状态并响应。你可以使用 `Thread.currentThread.hasPendingCancellation` 来检查是否有取消请求。

这种设计避免了强制终止线程可能带来的资源泄漏和数据不一致问题。

右侧的代码展示了一个线程如何响应取消请求并优雅地退出。
