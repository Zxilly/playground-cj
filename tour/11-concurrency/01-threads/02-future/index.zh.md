# 线程返回值（Future）

`spawn` 创建的线程可以返回值。代码块中最后一个表达式的值会成为线程的返回值，通过 `Future` 对象的 `.get()` 方法获取。

`Future<T>` 是一个泛型类型，`T` 是线程返回值的类型。调用 `.get()` 会阻塞当前线程，直到目标线程完成并返回结果。

这个模式非常适合并行计算——你可以同时启动多个计算任务，然后收集它们的结果。

右侧的代码展示了如何利用 `Future` 并行计算多个值，并将结果汇总。
