# 空接口

空接口是没有任何方法要求的接口。在仓颉中，`Any` 类型扮演了类似的角色——所有类型都是 `Any` 的子类型。

空接口可以持有任何类型的值，因此它可以用于需要存储任意类型的场景。但使用空接口意味着失去了类型信息，需要通过类型转换才能访问具体类型的方法。

在实际编程中，应该尽量使用具体的接口类型，只在确实需要存储任意类型时才使用空接口。过度使用空接口会削弱类型安全性。

右侧示例展示了如何使用 `Any` 类型存储不同类型的值。
