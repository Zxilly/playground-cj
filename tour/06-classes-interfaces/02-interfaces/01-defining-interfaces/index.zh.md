# 定义接口

接口（`interface`）定义了一组方法签名，但不提供实现。接口描述了类型应该具备的行为能力。

使用 `interface` 关键字定义接口，其中包含方法声明。接口也可以包含带有默认实现的方法。

接口是实现多态的重要机制。多个不相关的类可以实现同一个接口，从而被统一对待。与继承不同，一个类可以实现多个接口。

右侧示例定义了 `Printable` 和 `Describable` 两个接口。