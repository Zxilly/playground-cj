# 继承

仓颉支持类的继承，使用 `<:` 语法表示继承关系。子类继承父类的所有字段和方法，并可以添加新的字段和方法。

子类的构造器需要调用父类的构造器（通过 `super`）来初始化继承的字段。

继承实现了代码的复用——子类自动拥有父类的功能，同时可以扩展新的行为。仓颉只支持单继承，即一个类只能有一个父类。

右侧示例展示了 `Animal` 基类和 `Dog`、`Cat` 子类的继承关系。