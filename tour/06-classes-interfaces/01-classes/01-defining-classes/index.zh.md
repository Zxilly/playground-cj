# 定义类

类（`class`）是仓颉中的引用类型，用于创建具有状态和行为的对象。使用 `class` 关键字定义。

与结构体不同，类是引用类型。将类的实例赋值给另一个变量时，两个变量指向同一个对象，修改其中一个会影响另一个。

类可以包含字段（`var` 或 `let`）、构造器（`init`）和方法（`func`）。类的方法可以直接修改对象的可变字段，不需要 `mut` 修饰符。

右侧示例定义了一个简单的 `Animal` 类，包含名称和年龄字段以及一些方法。