# 属性（prop）

仓颉支持使用 `prop` 关键字定义计算属性。属性看起来像字段，但实际上通过 `get` 和可选的 `set` 方法来计算值。

只读属性只定义 `get()` 方法，可读写属性同时定义 `get()` 和 `set()` 方法。属性让你可以在不改变使用方式的前提下，控制值的读取和写入逻辑。

属性在访问时像普通字段一样使用点语法，但在背后会调用对应的 getter 或 setter 方法。

右侧示例展示了只读属性和可读写属性的定义与使用。
