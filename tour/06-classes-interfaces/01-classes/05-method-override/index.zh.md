# 方法重写

子类可以重写父类的方法来提供不同的实现。父类的方法需要用 `open` 修饰才能被重写，子类使用 `override` 关键字标记重写的方法。

`open` 和 `override` 的配合使用确保了方法重写是有意为之的，避免意外覆盖。如果父类方法没有标记为 `open`，子类无法重写它。

重写的方法在运行时会根据对象的实际类型来调用——即使通过父类类型的变量引用，也会调用子类的实现。这就是运行时多态。

右侧示例展示了方法重写和运行时多态的行为。