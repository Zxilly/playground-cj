# sealed 类

`sealed` 修饰符限制类只能在同一个包内被继承。包外的代码无法创建 `sealed` 类的子类。

这使得编译器能够在模式匹配时检查是否覆盖了所有可能的子类型，因为子类集合是已知且固定的。

`sealed` 类是介于 `open` 类和不可继承类之间的折中方案：允许受控的继承，同时防止外部代码随意扩展。

右侧示例展示了 `sealed` 类的定义。由于所有子类都在同一个文件中，`match` 表达式可以穷尽所有情况。
