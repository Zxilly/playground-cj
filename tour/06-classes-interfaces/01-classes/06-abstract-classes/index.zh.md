# 抽象类

抽象类使用 `abstract` 关键字定义，不能被直接实例化。它可以包含抽象方法（没有实现的方法）和具体方法（有实现的方法）。

子类必须实现所有抽象方法，否则子类也必须声明为抽象的。抽象类为一组相关的类定义了共同的接口和部分默认实现。

抽象类适合用于"模板方法"模式——在基类中定义算法的骨架，将某些步骤的实现延迟到子类。

右侧示例展示了一个抽象的 `Shape` 类，其子类必须实现 `area` 和 `name` 方法。
