# 练习：模式匹配

来练习一下模式匹配，实现一个 `classify` 函数来描述 `Option<Int64>` 的值。

给定一个 `Option<Int64>`，返回一个 `String`：

- `Some(n)` 且 `n > 0` → `"positive"`
- `Some(0)` → `"zero"`
- `Some(n)` 且 `n < 0` → `"negative"`
- `None` → `"none"`

在下方提供的 `func classify` 中实现它。
