# Tuple Patterns

`match` can destructure tuples. `case (0, 0)` matches a specific pair, while `case (x, y)` binds both elements.

Try changing `point` on line 2 to `(0, 5)` and see which branch matches.
