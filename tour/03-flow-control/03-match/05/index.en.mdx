# Type Patterns

`case x: Type =>` matches when the value is an instance of `Type` and binds it to `x`. This enables branching on the concrete type behind an interface.

Try adding a new class `Triangle <: Shape` and a matching `case` branch in `describe`.
