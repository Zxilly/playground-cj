# Match

`match` selects a branch based on the value of an expression. Each `case` specifies a pattern followed by `=>`.

Cangjie's `match` never falls through -- only the matched branch runs.

Try changing `day` on line 2 to `6` and see which case runs.
