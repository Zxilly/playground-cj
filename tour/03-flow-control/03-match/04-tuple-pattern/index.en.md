# Tuple Pattern

Tuple patterns match tuple values and can destructure multiple elements at once.

The syntax is `case (pattern1, pattern2, ...) =>`, where each position can use any pattern (value, binding, wildcard, etc.).

Tuple patterns are especially useful when you need to match multiple related values simultaneously.
