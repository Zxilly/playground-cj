# 基本 match

`match` 表达式是仓颉中强大的模式匹配工具，类似于其他语言的 `switch`，但功能更加丰富。

基本语法为 `match (表达式) { case 模式 => 代码 ... }`。程序会依次将待匹配值与每个 `case` 的模式比较，匹配成功则执行对应代码，然后退出 `match`。

`match` 要求匹配必须是穷尽的，即覆盖所有可能的值。通常可以用通配符 `_` 作为最后一个分支来匹配剩余所有情况。
