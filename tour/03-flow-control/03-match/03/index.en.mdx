# Binding Patterns

`case n =>` matches any value and binds it to the name `n` for use inside that branch.

This is useful as a catch-all that still lets you use the matched value.

Try changing `code` on line 2 to `500` and see what gets printed.
