# 穷举检查

仓颉编译器要求 `match` 表达式必须是穷尽的——所有 `case` 分支必须覆盖待匹配值的所有可能取值。

如果 `match` 不穷尽，编译器会报错。这个安全机制确保你不会遗漏任何情况。

常用的确保穷尽性的方式：

- 使用通配符 `_` 作为最后一个分支
- 使用绑定模式作为最后一个分支
- 对于 `enum` 类型，列出所有构造器

穷尽性检查是仓颉类型系统安全性的重要组成部分。
