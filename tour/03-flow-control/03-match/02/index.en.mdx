# Value Patterns

A value pattern matches when the input equals a specific literal: `case "A" => ...`.

The `|` operator combines several values in one case: `case 3 | 4 | 5 => "Spring"`.

Try changing `grade` on line 2 to `"D"` and see if it falls through to `_`.
