# Pattern Guards

`case n where n > 0 =>` adds a condition after the pattern. The branch runs only when both the pattern matches and the guard is true.

Note: Cangjie uses `where` for guards, not `if`.

Try changing the guard on line 5 to `where n > 0` and see which branch runs.
