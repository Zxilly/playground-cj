# Value Pattern

Value patterns (constant patterns) are the simplest form of pattern matching -- they match literal values directly.

You can match integer, floating-point, character, string, and boolean literals. Multiple values can be connected with `|` to match any one of them.

Value patterns use equality comparison to determine a match.
