# If

Cangjie's `if` statements work like most languages. The expression need not be surrounded by parentheses... just kidding -- parentheses around the condition are **required**.

The braces `{ }` are also required.

Try removing the parentheses around `(score >= 90)` on line 12 and observe the compiler error.
