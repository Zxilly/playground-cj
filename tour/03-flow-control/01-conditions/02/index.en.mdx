# If as Expression

In Cangjie, `if` is an expression -- it returns a value. Both branches must produce a value of the same type.

Try changing `temperature` on line 2 to `35` and see which value gets assigned to `weather`.
