# if-let

`if (let Some(v) <- x)` tests whether an `Option` contains a value. If it does, the inner value is bound to `v` and the branch executes.

You can add an extra condition with `&&`, as shown on line 20.

Try changing `Some(42)` to `None` on line 2 and see which branch runs.
