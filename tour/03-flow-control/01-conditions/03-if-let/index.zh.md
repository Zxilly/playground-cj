# if-let

仓颉支持在 `if` 条件中使用 `let` 模式匹配，这被称为 "if-let" 模式。

语法为 `if (let 模式 <- 表达式)`。当表达式的值与模式匹配成功时，条件为 `true`，并且模式中绑定的变量可以在 `if` 分支中使用。

这在处理 `Option` 类型时特别有用：可以在匹配成功时直接解包使用内部的值。

多个 `let` 模式可以用 `&&` 连接。注意：通过 `||` 连接时，不能绑定变量。
