# Range 表达式

区间（Range）表示一个有固定步长的序列，常用于 `for-in` 循环。

仓颉有两种区间字面量：

- **左闭右开**：`start..end`，包含 `start`，不包含 `end`
- **左闭右闭**：`start..=end`，包含 `start` 和 `end`

可以通过 `: step` 指定步长，例如 `0..10 : 2`。步长默认为 `1`，不能为 `0`。步长可以为负数，用于递减序列。
