# For-in

`for (x in collection)` iterates over each element of any iterable.

A `where` clause filters elements inline: `for (i in 0..10 where i % 2 == 0)`.

Try adding a few more elements to the `fruits` array on line 3 and run again.
