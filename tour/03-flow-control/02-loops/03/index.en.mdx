# Ranges

`0..5` is a half-open range (0 through 4). `0..=5` is a closed range (0 through 5).

A step is written with `: n`, such as `0..10 : 2`.

Try changing `..` to `..=` on line 4 and observe the last number printed.
