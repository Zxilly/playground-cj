# while 循环

`while` 表达式在条件为 `true` 时反复执行循环体。

语法为 `while (条件) { 循环体 }`。每次迭代前都会检查条件，条件为 `false` 时退出循环。

与 `if` 一样，条件必须是 `Bool` 类型。`while` 表达式的类型是 `Unit`。

仓颉也支持 `do-while` 循环，它先执行循环体，再检查条件，确保循环体至少执行一次。
