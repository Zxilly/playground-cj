# While

`while (condition) { body }` repeats a block as long as the condition is true.

`do { body } while (condition)` executes the body at least once before checking.

Try changing `i <= 10` to `i <= 5` on line 5 and see how the sum changes.
