# CFunc

`CFunc<(Params) -> Return>` is the type for C function pointers. There are three ways to create a `CFunc`: a `foreign` function, a `@C`-annotated Cangjie function, or a `CFunc`-typed lambda that does not capture variables. Calling a `CFunc` value requires an `unsafe` context.
