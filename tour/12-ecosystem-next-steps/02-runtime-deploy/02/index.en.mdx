# Deploy Runtime

A Cangjie binary dynamically links to the Cangjie runtime libraries by default, so they must be present on the target machine. Set `LD_LIBRARY_PATH` (Linux) or `PATH` (Windows) to point at the `runtime/lib/` directory from the SDK. Alternatively, compile with `--static` to produce a fully self-contained binary that bundles the runtime.
