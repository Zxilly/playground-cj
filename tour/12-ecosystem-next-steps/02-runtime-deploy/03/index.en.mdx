# CJNative and CJVM

Cangjie supports two compilation backends. CJNative compiles to native machine code for maximum performance, while CJVM compiles to JVM bytecode for Java ecosystem interoperability. The same Cangjie source code can target either backend by selecting the appropriate SDK, and you can use `@When[backend == "cjnative"]` for backend-specific code.
