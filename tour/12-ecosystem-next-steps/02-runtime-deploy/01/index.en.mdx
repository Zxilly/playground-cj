# Run with cjc

Compile a Cangjie source file to a native binary with `cjc main.cj -o main`, then run it with `./main`. Use `cjc -p src/` to compile all source files in a package directory at once. The resulting binary includes the Cangjie runtime, so it is ready to execute on the target platform.
