main() {
    println("Next steps after the tour:")
    println("1. Save one example as its own .cj file")
    println("2. Compile it with cjc")
    println("3. Start a project with cjpm init when it grows")
}
