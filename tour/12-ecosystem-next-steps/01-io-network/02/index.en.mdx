# HTTP and Socket Overview

The `stdx.net.http` package provides `ClientBuilder` and `ServerBuilder` for HTTP client and server programming. For transport-layer networking, `std.net` offers `TcpSocket`, `TcpServerSocket`, and `UdpSocket`. These networking APIs use blocking I/O on lightweight Cangjie threads, so a blocked thread does not consume a system thread.
