# HTTP 与 Socket 概览

`stdx.net.http` 包提供了 `ClientBuilder` 和 `ServerBuilder`，用于 HTTP 客户端和服务端编程。在传输层网络方面，`std.net` 提供了 `TcpSocket`、`TcpServerSocket` 和 `UdpSocket`。这些网络 API 使用仓颉轻量线程上的阻塞 I/O，因此被阻塞的线程不会占用系统线程。
