# I/O Overview

Cangjie's I/O system is built on streams. `InputStream` and `OutputStream` are the base interfaces for reading and writing byte data. In-memory streams like `ByteBuffer` let you work with byte data without touching the file system. Wrap any input stream with `StringReader` to conveniently read text line by line.
