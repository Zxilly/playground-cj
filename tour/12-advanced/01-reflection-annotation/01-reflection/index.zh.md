# TypeInfo 与反射

仓颉支持**反射**机制，允许程序在运行时获取类型的信息。通过 `import std.reflect.*` 导入反射库。

`TypeInfo` 是反射的核心类型，可以获取类型的名称、方法列表、属性等信息。你可以使用 `TypeInfo.of<T>()` 来获取类型 `T` 的类型信息。

反射的常见用途：
- 动态检查类型信息
- 构建通用的序列化/反序列化框架
- 实现插件系统

注意：反射会带来一定的性能开销，应只在确实需要运行时类型信息时使用。

右侧的代码展示了如何使用反射获取类型信息。