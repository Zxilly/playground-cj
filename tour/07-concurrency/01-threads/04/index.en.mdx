# Sleep

`sleep` pauses the current thread for a specified duration while other threads continue running.

```cangjie
sleep(200 * Duration.millisecond)
```

`Duration.millisecond` is the time unit. The countdown prints each number, sleeps 200 ms, then prints "go".

Change the sleep duration to `500 * Duration.millisecond` and observe the slower countdown.
