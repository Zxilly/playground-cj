# Exercise: Concurrent Counter

Use a `Mutex` to implement a thread-safe counter. Spawn 10 threads, each incrementing the counter 100 times. Print the final value — it should always be 1000.

Without the mutex, the result is unpredictable. Run it and observe the difference.
