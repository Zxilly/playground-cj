# Generic Classes

A generic class uses reference semantics and supports mutable state. `Stack<T>` is a classic example -- one implementation handles any element type.

The type parameter flows through fields, method parameters, and return types. Two `Stack` instances with different type arguments are completely independent types.

Create a `Stack<Int64>`, push several numbers, then pop them and print the results.
