# HashSet

`HashSet` is a collection that does not allow duplicate elements. It is implemented using a hash table, providing efficient insertion, deletion, and lookup operations.

Common operations include `put` to add elements, `contains` to check membership, and `remove` to delete elements. Adding the same element twice has no effect.

Sets also support mathematical set operations like union, intersection, and difference, which are useful for deduplication and data comparison.
