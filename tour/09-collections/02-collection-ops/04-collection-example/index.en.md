# Collection Example

This section demonstrates a practical word counting example that combines collection operations. This is a classic programming exercise showing the combined use of `HashMap`, `ArrayList`, and sorting.

The program splits text into words, uses a `HashMap` to count each word's occurrences, then outputs the results sorted by frequency.

This example illustrates typical collection usage patterns in real programming: collecting data, statistical analysis, and sorted display.
