# 集合转换

不同类型的集合之间可以相互转换。常见的转换包括将数组转为 `ArrayList`、将 `ArrayList` 转为 `HashSet` 以去重，或从集合构建 `HashMap`。

转换操作允许你选择最适合当前需求的数据结构。例如，收集数据时使用 `ArrayList`，查重时转为 `HashSet`，需要查找时构建 `HashMap`。

合理使用集合转换可以让你的代码更高效，同时保持简洁。
