# 算术运算符重载

仓颉允许你为自定义类型重载运算符，使其支持 `+`、`-`、`*` 等算术操作。运算符重载通过在类型中定义 `operator func` 来实现。

例如，`operator func +(rhs: Vec2): Vec2` 定义了 `+` 运算符。当你写 `a + b` 时，实际上调用的是 `a` 上的 `+` 方法，`b` 作为参数传入。

常见的可重载算术运算符包括：
- `+`（加法）
- `-`（减法）
- `*`（乘法）
- `/`（除法）

运算符重载让自定义类型的使用更加自然和直观。右侧的代码为二维向量 `Vec2` 实现了基本的算术运算。