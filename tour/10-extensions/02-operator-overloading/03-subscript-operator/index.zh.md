# 下标运算符重载

**下标运算符** `[]` 允许你像访问数组一样通过索引访问自定义类型的元素。通过 `operator func [](index: T): R` 来定义。

下标运算符在以下场景非常有用：
- 自定义集合类型
- 矩阵或多维数据结构
- 字典或映射类型

你可以同时定义只读和读写版本的下标运算符。右侧的代码展示了一个简单的 `Matrix` 类型，使用下标运算符来访问矩阵中的元素。