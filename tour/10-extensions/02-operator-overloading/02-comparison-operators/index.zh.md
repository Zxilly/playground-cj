# 比较运算符重载

除了算术运算符，你还可以重载**比较运算符**，使自定义类型支持 `==`、`!=`、`<`、`>` 等比较操作。

`==` 运算符通过 `operator func ==(rhs: T): Bool` 来定义。定义了 `==` 后，`!=` 通常也可以自动使用。

`<` 运算符通过 `operator func <(rhs: T): Bool` 来定义。类似地，定义了 `<` 后，其他比较运算符（`>`、`<=`、`>=`）也可以推导出来。

比较运算符在排序、查找等场景中非常有用。右侧的代码为 `Temperature` 类型实现了比较运算符。