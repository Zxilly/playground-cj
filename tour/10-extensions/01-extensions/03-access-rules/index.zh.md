# 扩展中的访问规则

使用扩展时，有一些重要的访问规则需要了解：

- 扩展**可以**添加新的方法
- 扩展**不能**添加存储属性（成员变量）
- 扩展中只能访问类型的**公开**成员
- 扩展可以访问传入参数和 `this` 的公开接口

这些限制确保了扩展不会破坏原始类型的封装性。扩展是在类型外部定义的，因此只能通过公开接口与类型交互。

右侧的代码展示了在扩展中如何通过公开方法与类型交互。扩展为 `Rectangle` 添加了计算面积和描述的功能，但只使用了公开的属性。
