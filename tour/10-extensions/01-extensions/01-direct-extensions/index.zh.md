# 直接扩展

仓颉允许你通过**扩展**为已有类型添加新的方法，而无需修改原始类型的定义。这是一种非常强大的特性，可以让你为任何类型添加功能。

**直接扩展**是最简单的扩展形式，不需要实现任何接口。使用 `extend` 关键字后跟类型名称，然后在大括号中定义新的方法。

在扩展中，你可以使用 `this` 来引用被扩展类型的实例。扩展可以添加实例方法和静态方法。

右侧的代码展示了如何为 `Int64` 类型添加 `isEven` 和 `isPositive` 方法。扩展定义后，你可以像调用原生方法一样使用这些新方法。