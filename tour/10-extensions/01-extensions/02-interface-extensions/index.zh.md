# 接口扩展

除了直接扩展，仓颉还支持**接口扩展**——让已有类型实现新的接口。使用 `extend TypeName <: InterfaceName` 语法，你可以为类型添加接口的实现。

这在以下场景非常有用：
- 你想让已有类型符合某个接口的约定
- 类型来自第三方库，你无法修改它的定义
- 你想为类型添加更丰富的行为

接口扩展必须实现接口中声明的所有方法。通过这种方式，你可以让任何类型融入你设计的接口体系中。

右侧的代码定义了一个 `Describable` 接口，然后通过扩展让 `Int64` 和 `Bool` 类型都实现了该接口。