# Import as

`import pkg.Name as Alias` renames an imported declaration. This resolves name conflicts between packages.

After renaming, only the alias is available -- the original name cannot be used.

Try renaming `List` to `MyList` and updating the usage to match.
