# Visibility

`public` makes a declaration visible everywhere. `internal` (the default) limits it to the current package and subpackages. `private` restricts it to the current file.

There is also `protected`, which limits visibility to the current module.

Try changing `publicGreet` from `public` to `private` and calling it from another file -- you will get a compile error.
