# Imports

`import std.collection.*` imports everything from a package. `import std.collection.ArrayList` imports a single item.

The `std.core` package is implicitly imported in every file, so types like `String` and `Array` are always available.

Try changing `import std.collection.ArrayList` to `import std.collection.*` and observe that the code still works.
