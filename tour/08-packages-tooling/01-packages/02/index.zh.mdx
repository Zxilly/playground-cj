# 导入

`import std.collection.*` 导入包中的所有内容。`import std.collection.ArrayList` 只导入单个项。

`std.core` 包在每个文件中被隐式导入，因此 `String` 和 `Array` 等类型始终可用。

试着将 `import std.collection.ArrayList` 改为 `import std.collection.*`，观察代码仍然正常工作。
