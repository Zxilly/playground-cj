# cjpm build and run

`cjpm build` compiles the current project. `cjpm run` compiles and runs it in one step.

`cjpm test` discovers and runs unit tests marked with `@Test` and `@TestCase`.

Try modifying the `add` function to subtract instead, then run the code to see the change.
