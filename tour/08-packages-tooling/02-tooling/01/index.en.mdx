# cjc

`cjc` is the Cangjie compiler. Run `cjc hello.cj -o hello` to compile a single file.

Use `-p src/` to compile an entire package directory.

Try adding a second `println` call and running the code again.
