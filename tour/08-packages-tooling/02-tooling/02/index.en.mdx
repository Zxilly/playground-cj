# cjpm init

`cjpm init` creates a new project with a `cjpm.toml` file and `src/main.cj`.

The `[package]` section in `cjpm.toml` sets the module name, version, and output type.

Try changing the version in the `cjpm.toml` comment from `"1.0.0"` to `"2.0.0"`.
