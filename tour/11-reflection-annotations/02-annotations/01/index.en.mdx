# Annotation Classes

An annotation is a structured marker that attaches metadata to a declaration. Annotations are defined with `@` and used to drive compile-time or runtime behavior.

Built-in annotations like `@Test`, `@TestCase`, and `@Assert` are used by the testing framework. Custom annotation classes let you define your own metadata.
