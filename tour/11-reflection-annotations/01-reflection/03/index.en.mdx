# Calling Methods

`TypeInfo` provides access to a type's methods at runtime. You can look up a method by name and invoke it dynamically, without knowing the concrete type at compile time.

This enables generic frameworks — such as serializers or test runners — to discover and call methods on arbitrary objects.

Try calling a different method name and observe the runtime error when it does not exist.
