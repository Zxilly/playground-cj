# 泛型函数

泛型函数允许你编写可以操作多种类型的函数，而无需为每种类型都编写一个版本。通过在函数名后使用尖括号 `<T>` 声明类型参数，函数体内就可以使用该类型参数。

调用泛型函数时，编译器通常能够根据传入的参数自动推断类型参数，因此你不需要显式指定类型。当然，你也可以在调用时显式提供类型参数。

泛型函数是代码复用的重要工具。右侧示例展示了一个 `identity` 函数和一个 `makePair` 函数，它们可以处理任意类型的参数。
