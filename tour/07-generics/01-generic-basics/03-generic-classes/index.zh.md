# 泛型类

泛型类与泛型结构体类似，但类支持继承和引用语义。泛型类常用于实现可复用的数据结构，如栈、队列等。

在下面的示例中，我们实现了一个泛型 `Stack` 类。它内部使用 `ArrayList` 来存储元素，并提供 `push`、`pop` 和 `peek` 等方法。由于使用了泛型，这个栈可以存储任意类型的元素。

泛型类的方法可以自由地使用类的类型参数，使得整个类的 API 都是类型安全的。
