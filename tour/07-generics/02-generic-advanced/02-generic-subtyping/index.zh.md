# 泛型子类型

泛型类型之间的子类型关系是一个重要概念。如果 `Dog` 是 `Animal` 的子类型，那么 `Box<Dog>` 是否是 `Box<Animal>` 的子类型呢？

这取决于泛型类型的型变（variance）规则。在仓颉中，泛型类型默认是不变的（invariant），也就是说 `Box<Dog>` 和 `Box<Animal>` 之间没有子类型关系，即使 `Dog <: Animal`。

要在需要基类型泛型的地方使用子类型泛型，通常需要通过泛型接口和类型约束来实现多态。右侧示例展示了如何利用接口实现泛型的多态行为。