// 仓颉融合了多种现代语言的优秀特性
main() {
    // 类似 Rust/Kotlin 的不可变变量声明
    let language = "Cangjie"
    
    // 类型推断，编译器自动推导类型
    let year = 2024
    let isNew = true
    
    // 字符串模板插值
    println("语言: ${language}")
    println("年份: ${year}")
    println("是新品吗? ${isNew}")
}