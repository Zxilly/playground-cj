# Using the Compiler Locally (Optional)

If you want to practice the examples on your own machine, first [download and install the Cangjie compiler](https://cangjie-lang.cn/download), save the code as a `.cj` file, and then compile it with `cjc`:

```
cjc hello.cj -o hello && ./hello
```
