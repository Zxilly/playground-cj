# Hello, 世界

欢迎来到[仓颉编程语言](https://cangjie-lang.cn)之旅。

本教程分为若干模块，可通过侧边栏访问。你将在教程中找到一系列幻灯片和练习。

教程是交互式的。现在点击 <Highlight target="run">运行</Highlight> 按钮（或按 `Shift` + `Enter`），将程序发送到远程服务器编译并运行。结果显示在代码下方。

修改程序，再运行一次。

准备好继续时，点击下方的 <Highlight target="next">右箭头</Highlight>，或按 `PageDown` 键。
