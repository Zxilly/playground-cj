// The entry point of a Cangjie program is the main function
main() {
    // Use let to declare an immutable variable
    let greeting = "Hello"
    let name = "Cangjie"
    
    // Use ${} for string interpolation
    let message = "${greeting}, ${name}!"
    
    // Output to standard output
    println(message)
}