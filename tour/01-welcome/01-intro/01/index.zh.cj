// 仓颉程序的入口是 main 函数
main() {
    // 使用 let 声明不可变变量
    let greeting = "你好"
    let name = "仓颉"
    
    // 使用 ${} 进行字符串插值
    let message = "${greeting}，${name}！"
    
    // 输出到标准输出
    println(message)
}