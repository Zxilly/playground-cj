main() {
    // Playground runs your code in a remote sandbox
    let x = 10
    let y = 20
    
    // Perform simple calculations
    let sum = x + y
    let product = x * y
    
    // Output calculation results
    println("Values: x = ${x}, y = ${y}")
    println("Addition: ${x} + ${y} = ${sum}")
    println("Multiplication: ${x} * ${y} = ${product}")
}