main() {
    // Playground 在远程沙盒中运行你的代码
    let x = 10
    let y = 20
    
    // 执行简单计算
    let sum = x + y
    let product = x * y
    
    // 输出计算结果
    println("数值: x = ${x}, y = ${y}")
    println("加法: ${x} + ${y} = ${sum}")
    println("乘法: ${x} * ${y} = ${product}")
}