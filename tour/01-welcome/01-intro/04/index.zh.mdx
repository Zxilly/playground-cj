# Playground

本教程基于仓颉 Playground 构建，这是一个运行在远程服务器上的 Web 服务。

该服务接收仓颉程序，在沙盒中编译、链接并运行，然后返回输出结果。

在 Playground 中运行的程序有以下限制：

- 执行时间和内存使用有上限。

可直接在浏览器中访问[仓颉 Playground](https://cangjie-lang.cn/playground) 在线体验。
