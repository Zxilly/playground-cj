# Hello World

每个仓颉程序都从 `main` 函数开始执行。`main()` 是程序的入口点，程序启动时会自动调用它。

`println()` 函数用于将内容输出到控制台，并在末尾自动换行。你也可以使用 `print()` 函数输出内容但不换行。

仓颉的 `main` 函数不需要显式声明返回类型。函数体由一对花括号 `{}` 包围，其中包含要执行的代码。

试着修改下面的输出内容，然后运行程序看看效果。
