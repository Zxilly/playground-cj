# 闭包

闭包是能够捕获其定义环境中变量的函数。Lambda 表达式和嵌套函数都可以形成闭包。

当 Lambda 引用了外部作用域的变量时，它会"捕获"这些变量。被捕获的变量在 Lambda 被调用时仍然可以访问，即使原始作用域已经结束。

闭包的一个经典应用是创建"工厂函数"——返回一个函数的函数。每次调用工厂函数都会创建一个新的闭包，拥有自己独立的捕获变量。

右侧示例展示了闭包如何捕获变量，以及如何利用闭包创建计数器。