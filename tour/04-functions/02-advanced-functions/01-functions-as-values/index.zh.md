# 函数作为值

在仓颉中，函数是一等公民（first-class），可以像普通值一样被赋值给变量、作为参数传递、或从函数中返回。

函数类型的写法是 `(参数类型) -> 返回类型`。例如，`(Int64) -> Int64` 表示接受一个 `Int64` 参数并返回 `Int64` 的函数类型。

将函数赋值给变量时，直接使用函数名即可（不带括号）。变量的类型就是对应的函数类型。

函数作为参数传递是高阶编程的基础。右侧示例中，`apply` 函数接受一个函数参数和一个整数参数，将函数应用到整数上。
