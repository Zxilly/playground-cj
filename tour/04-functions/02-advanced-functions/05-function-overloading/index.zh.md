# 函数重载

仓颉支持函数重载，即可以定义多个同名函数，只要它们的参数列表不同（参数数量或参数类型不同）。

编译器会根据调用时提供的实参类型和数量来自动选择匹配的函数版本。这让你可以用统一的函数名来处理不同类型的输入。

函数重载提供了一种自然的多态方式。调用者不需要记住不同的函数名，编译器会在编译时解析出正确的版本。

右侧示例定义了多个 `format` 函数，分别处理整数、浮点数和字符串参数。