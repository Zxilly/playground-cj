# 尾随 Lambda

当函数的最后一个参数是函数类型时，可以使用尾随 Lambda 语法。调用时将 Lambda 写在括号之外，使代码更加简洁自然。

如果函数只有一个参数且该参数是函数类型，甚至可以省略括号，直接写 Lambda。

尾随 Lambda 语法在集合操作、回调函数、构建 DSL 等场景中非常常用。它让代码读起来更像自然语言。

右侧示例展示了如何使用尾随 Lambda 语法调用接受函数参数的函数。