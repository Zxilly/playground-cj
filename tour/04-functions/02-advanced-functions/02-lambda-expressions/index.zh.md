# Lambda 表达式

Lambda 表达式是匿名函数的简洁写法。语法为 `{ 参数列表 => 函数体 }`，其中参数列表包含参数名和类型。

当 Lambda 没有参数时，使用 `{ => 函数体 }` 的形式。当函数体只有一个表达式时，该表达式的值自动作为 Lambda 的返回值。

Lambda 表达式常用于需要传递简短函数的场景，比如作为高阶函数的参数。它们避免了为一次性使用的简单逻辑定义独立函数的繁琐。

右侧示例展示了不同形式的 Lambda 表达式，包括赋值给变量和作为函数参数使用。