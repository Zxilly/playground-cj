# 命名参数

仓颉支持命名参数，使函数调用更加清晰易读。在定义函数时，在参数名后加 `!` 标记该参数为命名参数。

调用时，命名参数需要在调用处显式写出参数名，形如 `name: value`。这样做的好处是代码的可读性更强，尤其是当函数有多个相同类型的参数时，命名参数可以避免参数顺序混淆。

命名参数可以按任意顺序传递，因为调用者已经明确指定了每个参数的名称。

右侧示例中，`createUser` 函数使用命名参数来清楚地表示每个参数的含义。