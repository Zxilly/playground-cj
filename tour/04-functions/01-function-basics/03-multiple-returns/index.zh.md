# 多返回值（元组）

仓颉中函数只能有一个返回值，但可以通过返回元组来实现多返回值的效果。元组是将多个值组合在一起的轻量级数据结构。

元组类型的写法是将各元素类型放在圆括号中，用逗号分隔，例如 `(Int64, Int64)`。创建元组只需将值放在圆括号中即可。

访问元组中的元素使用从零开始的索引，例如 `.0` 获取第一个元素，`.1` 获取第二个元素。也可以使用解构赋值一次性将元组的各个元素绑定到不同的变量上。

右侧示例中，`minMax` 函数返回数组中的最小值和最大值，以元组的形式返回。
