# 参数与返回值

函数的参数定义了函数接受的输入。每个参数都有名称和类型，多个参数之间用逗号分隔。

函数的返回值类型写在参数列表之后，用冒号分隔。当函数体的最后一个表达式就是返回值时，可以省略 `return` 关键字。

右侧示例展示了几种不同的参数和返回值组合：`multiply` 接受两个参数并返回乘积；`isEven` 接受一个整数并返回布尔值；`describe` 接受一个字符串参数并返回格式化后的描述字符串。

仓颉中所有参数都是按值传递的。对于值类型（如 `Int64`、`Bool`），函数接收的是值的副本。
