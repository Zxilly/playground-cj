# 定义函数

在仓颉中，使用 `func` 关键字来定义函数。函数是组织代码的基本单元，它接受输入参数并可返回结果。

函数定义的基本语法为：`func 函数名(参数名: 参数类型): 返回类型 { 函数体 }`。如果函数没有返回值，返回类型为 `Unit`，此时可以省略返回类型声明。

右侧的示例定义了一个 `add` 函数，接受两个 `Int64` 类型的参数，返回它们的和。注意，当函数体只有一个表达式时，该表达式的值会自动作为返回值，也可以使用 `return` 关键字显式返回。

你也可以定义没有参数或没有返回值的函数。`sayHello` 函数不接受任何参数，仅执行打印操作。
