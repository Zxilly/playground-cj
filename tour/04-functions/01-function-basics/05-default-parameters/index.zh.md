# 默认参数值

命名参数可以设置默认值，这样调用时可以省略该参数。默认值在参数类型后使用 `=` 指定。

当调用者不提供有默认值的命名参数时，函数将使用默认值。调用者也可以显式传入值来覆盖默认值。

默认参数非常适合那些大多数情况下使用固定值、但偶尔需要自定义的场景。它可以减少函数调用时需要提供的参数数量，让常见的调用更加简洁。

右侧示例展示了 `greet` 函数，其中 `greeting` 参数有默认值 `"Hello"`，调用时可以省略或覆盖。