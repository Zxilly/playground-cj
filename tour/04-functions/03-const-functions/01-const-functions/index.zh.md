# const 函数与编译期求值

仓颉支持 `const` 函数，这类函数可以在编译期求值。使用 `const` 修饰的函数，当其参数在编译时已知时，编译器会在编译阶段直接计算出结果。

`const` 函数的限制是函数体只能包含编译期可以计算的表达式，不能有副作用（如 I/O 操作）。

编译期求值的好处是运行时零开销——计算结果在编译时就已经确定。这对于常量表达式、数学计算等场景非常有用。

右侧示例定义了一个 `const` 修饰的阶乘函数，它的结果在编译期就可以计算出来。在 `main` 中我们同时展示了编译期和运行期的调用。