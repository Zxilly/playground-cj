# 基本枚举

枚举（`enum`）用于定义一组命名的常量值。使用 `enum` 关键字定义，各成员之间用 `|` 分隔。

枚举成员通过 `枚举名.成员名` 的方式访问。枚举常用于表示有限的、互斥的选项集合，比如方向、颜色、状态等。

可以使用 `match` 表达式对枚举值进行模式匹配，确保处理所有可能的情况。这是使用枚举的推荐方式。

右侧示例定义了一个 `Direction` 枚举，并使用 `match` 来根据方向执行不同的操作。
