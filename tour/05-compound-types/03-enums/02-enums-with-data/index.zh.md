# 带数据的枚举

仓颉的枚举成员可以携带关联数据。每个成员可以有不同类型和数量的关联值，这使得枚举成为一种强大的数据建模工具。

带数据的枚举成员在定义时指定数据类型，创建时提供对应的值。在 `match` 时，可以通过模式匹配提取关联数据。

这种模式类似于其他语言中的"标签联合"（tagged union）或"代数数据类型"（ADT），非常适合表示多种可能形态的数据。

右侧示例定义了 `Shape` 枚举，其中每种形状携带不同的尺寸数据。