# Option 类型

`Option<T>` 是仓颉标准库中的一个重要枚举类型，用于表示一个值可能存在（`Some(value)`）或不存在（`None`）。

`Option` 类型是处理可能为空的值的安全方式，避免了空指针异常。通过模式匹配，你必须显式处理值不存在的情况。

使用 `Option` 类型可以让代码更安全、更清晰地表达意图——告诉调用者这个值可能不存在，需要做好处理。

右侧示例展示了如何使用 `Option` 来安全地处理可能失败的查找操作。
