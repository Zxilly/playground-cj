# 元组

元组是一种将多个不同类型的值组合在一起的轻量级数据结构。元组使用圆括号创建，元素之间用逗号分隔。

元组的类型由其元素的类型决定，例如 `(Int64, String)` 是一个包含整数和字符串的元组类型。元组的元素可以是不同类型的。

访问元组元素使用方括号加索引，从 `0` 开始。也可以使用解构赋值将元组的元素分别绑定到不同的变量。

元组适合在需要返回多个值或临时组合几个相关值时使用，不需要定义专门的结构体。
