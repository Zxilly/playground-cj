# 数组

数组是存储相同类型元素的有序集合。在仓颉中，数组类型写作 `Array<元素类型>`，可以用字面量 `[元素1, 元素2, ...]` 来创建。

数组元素通过方括号加索引访问，索引从 `0` 开始。使用 `.size` 属性获取数组的长度。

可以使用 `for-in` 循环遍历数组中的所有元素。数组也支持使用构造函数创建，例如 `Array<Int64>(5, {i => i * 2})` 创建一个包含 5 个元素的数组。

右侧示例展示了数组的基本创建和访问方式。