# 结构体方法

结构体可以定义方法来操作其数据。方法在结构体内部使用 `func` 关键字定义，可以通过 `this` 访问结构体的字段。

普通方法是只读的，不能修改结构体的字段。如果需要修改字段，需要使用 `mut` 修饰符（下一节介绍）。

方法通过点语法（`.`）调用，例如 `instance.methodName()`。方法可以有参数和返回值，就像普通函数一样。

右侧示例中，`Circle` 结构体定义了计算面积和周长的方法。
