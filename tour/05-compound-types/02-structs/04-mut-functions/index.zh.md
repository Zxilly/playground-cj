# mut 函数

由于结构体是值类型，普通方法不能修改结构体的字段。要修改字段，需要将方法声明为 `mut func`。

`mut` 方法只能在 `var` 声明的变量上调用，因为 `let` 声明的变量是不可变的，不允许修改。

`mut` 方法实际上是在修改调用者自身的值。这与类的方法不同——类是引用类型，方法可以直接修改对象状态。

右侧示例中，`Point` 结构体的 `translate` 方法使用 `mut` 修饰符来移动点的坐标。