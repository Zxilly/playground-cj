# 定义结构体

结构体（`struct`）是仓颉中的值类型，用于将相关的数据组合在一起。使用 `struct` 关键字定义结构体，其中可以包含字段和方法。

结构体的字段使用 `let`（不可变）或 `var`（可变）声明。每个字段需要指定类型。

作为值类型，结构体在赋值或传参时会被复制。这意味着修改副本不会影响原始值。

右侧示例定义了一个 `Point` 结构体，包含 `x` 和 `y` 两个坐标字段。
