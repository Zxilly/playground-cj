# 异常类型层次

仓颉使用异常来处理运行时错误。`Exception` 是所有异常的基类，它提供了 `message` 属性来描述错误信息。

常见的内置异常类型包括数组越界、类型转换失败、算术错误等。所有异常都继承自 `Exception` 基类，形成一个层次结构。

了解异常的层次结构有助于你编写更精确的错误处理代码。你可以捕获特定类型的异常来处理特定的错误情况，也可以捕获基类 `Exception` 来处理所有异常。
