# throw 与 try-catch

`throw` 关键字用于抛出异常，`try-catch` 用于捕获和处理异常。当代码执行到 `throw` 语句时，当前函数的执行会立即停止，异常会沿调用栈向上传播，直到被某个 `catch` 块捕获。

`catch` 块可以指定要捕获的异常类型。如果有多个 `catch` 块，它们会按顺序匹配，因此应该将更具体的异常类型放在前面。

未被捕获的异常会导致程序终止。良好的异常处理能让程序在遇到错误时优雅地恢复。