# 异常 vs Option 选择

异常和 `Option` 都可以处理错误情况，但它们适用于不同的场景。

**使用异常**：当错误是意外的、不应在正常流程中发生时。例如文件损坏、网络中断、非法参数等。异常可以携带详细的错误信息。

**使用 Option**：当"无值"是一个合理的、可预期的结果时。例如在集合中查找元素、解析可能无效的输入等。`Option` 迫使调用者显式处理缺失的情况。

选择正确的错误处理方式能让你的代码更清晰、更易维护。