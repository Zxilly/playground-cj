# Option 处理空值

`Option<T>` 类型用于表示一个值可能存在也可能不存在。它有两个变体：`Some(value)` 表示有值，`None` 表示无值。

与其他语言中的 null 不同，`Option` 是类型安全的。编译器会强制你处理值不存在的情况，从而避免空指针错误。

当一个函数可能无法返回有效结果时，返回 `Option<T>` 比返回一个特殊值（如 -1 或空字符串）更加清晰和安全。
