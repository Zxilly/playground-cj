# Option 与模式匹配

模式匹配是处理 `Option` 值最常用的方式。通过 `match` 表达式，你可以同时处理 `Some` 和 `None` 两种情况，并且在 `Some` 分支中直接提取内部的值。

编译器会确保你处理了所有可能的情况，如果遗漏了某个分支会给出警告。这种穷尽性检查是 `Option` 类型安全的重要保障。

模式匹配不仅可以用在 `match` 表达式中，还可以用在 `if-let` 等场景中，提供更简洁的语法。
