# cangjie-editor module

`components/` contains TSX rendering components for the editor shell, runner, and status UI.

`hooks/` contains reusable client hooks and state-to-view adapters used by the editor components.

`context/` contains the editor bridge context object, provider component, and consumer hook as separate files.
